<% imports.forEach(function(param){%>import { I<%= param.name %> } from './I<%= param.name %>';
<% }); %>
export interface I<%= className;%> {
    <% parameters.forEach(function(param){ %><%= param.name %>:<%= ((param.isClass)?'I':'')+param.type %>;
    <% }); %>
}